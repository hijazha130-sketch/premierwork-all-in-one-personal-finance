import { Card } from "@/components/ui";

/**
 * The 50/30/20 lens (Plan). Placeholder in Step 5; the real needs/wants/savings
 * split arrives in Step 6.
 */
export function FiftyThirtyTwentyView() {
  return (
    <Card>
      <div className="text-xs font-semibold uppercase tracking-widest text-gold mb-2">50/30/20</div>
      <p className="text-muted">The needs / wants / savings split is coming to this view next.</p>
    </Card>
  );
}
